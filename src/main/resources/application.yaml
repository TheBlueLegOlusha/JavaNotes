server:
  port: 5000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/notesdb
    username: pavel
    password: 241219939
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          time_zone: UTC+3
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true